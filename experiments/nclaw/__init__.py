"""NCLaw-matched comparison suite (see docs in the video2sim plan doc).

Migrated from video2sim/sim so the comparison is reproducible from this repo
alone plus a clone of NCLaw (for their meshes and configs). Set NCLAW_DIR to
that clone; the default looks for a sibling or parent NCLaw directory.
"""
